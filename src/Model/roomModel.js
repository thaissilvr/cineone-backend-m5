let id = 0
class Room {
    constructor(time, type, max_capacity) {  
            this.time = time
            this.type = type
            this.max_capacity = max_capacity
    }
}

module.exports = Room
